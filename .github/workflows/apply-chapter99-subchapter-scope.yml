name: Apply Chapter 99 subchapter-scope patch

on:
  push:
    branches:
      - preview-import-calculator
    paths:
      - .github/workflows/apply-chapter99-subchapter-scope.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: preview-import-calculator
          fetch-depth: 0
      - name: Patch parser
        run: python scripts/patch-chapter99-subchapter-scope.py
      - name: Commit parser fix
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add scripts/build-chapter99-index.py
          git commit -m 'Scope Chapter 99 notes by subchapter'
          git push origin HEAD:preview-import-calculator
